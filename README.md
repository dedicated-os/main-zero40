# main-zero40

A bare-bones Tina Linux firmware for the MagicX Zero40. Includes SDL2, evtest, tree, and adb. Attempts to launch `main.sh` at the root of a second SD card and powers off if absent. For use with [Dedicated OS](https://github.com/dedicated-os/dedicated-zero40).
